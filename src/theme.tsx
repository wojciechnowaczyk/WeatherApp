import {createTheme} from '@rneui/themed';

export const theme = createTheme({
  lightColors: {
    primary: '#FFE382',
    secondary: '#FFAD84',
    background: '#FFF78A',
    white: '#FFFFFF',
  },
  darkColors: {
    primary: 'blue',
  },
});
